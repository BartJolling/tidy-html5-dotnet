#pragma once

namespace TidyHtml5Dotnet
{
    public enum class RepeatedAttributeModes
    {
        KeepFirst = 0, /**< Keep the first instance of an attribute */
        KeepLast       /**< Keep the last instance of an attribute */
    };
}