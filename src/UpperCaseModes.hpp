#pragma once

namespace TidyHtml5Dotnet
{
    public enum class UpperCaseModes
    {
        No = 0,   /**< Don't uppercase. */
        Yes,      /**< Do uppercase. */
        Preserve  /**< Preserve case. */
    };
}